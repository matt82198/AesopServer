spring:
  application:
    name: AesopServer-Test

server:
  port: 8871

aesop:
  brain:
    aesop-root: "./test-fixtures/aesop"
    conductor-root: "./test-fixtures/conductor3"
  db-path: ""
  inbox-path: "./test-inbox.jsonl"
  server-token: "test-secret-token"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
